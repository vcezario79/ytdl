name: ytdlp_ui
description: A simple UI for yt-dlp audio downloads.
publish_to: none
version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  file_picker: ^8.1.2
  path: ^1.9.0

flutter:
  uses-material-design: true
